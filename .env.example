# Inventory application environment variables
# Copy to .env and export, or prefix your commands.

# Path to JSON storage file (auto-created)
STORE_FILE=data/products.json

# Optional config file (overridden by STORE_FILE when both set)
APP_CONFIG=config/mini-inventory.json

# HTTP server port
PORT=3000

